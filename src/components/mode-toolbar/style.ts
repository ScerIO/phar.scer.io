import styled from 'styled-components'
import UIRadioGroup from '@material-ui/core/RadioGroup'

const RadioGroup = styled(UIRadioGroup)`
  && {
    display: block;
  }
`
export default {
  RadioGroup,
}
